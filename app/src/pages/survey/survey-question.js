
module.exports = function(params) {
    var self = this;
    self.element = params.element;
    self.publishedObs = params.publishedObs;
    self.indexObs = params.indexObs;
    self.surveyObs = params.surveyObs;
};