var utils = require('../../../utils');
var serverService = require('../../../services/server_service');

module.exports = function(params) {
    var self = this;

    self.emailObs = params.viewModel.emailObs;

    self.signOut = function(vm, event) {
        
    }
};
